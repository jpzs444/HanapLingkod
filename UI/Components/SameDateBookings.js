import { StyleSheet, Text, View } from 'react-native'
import React from 'react'

const SameDateBookings = ({props}) => {

    console.log(props)
  return (
    <View>
      <Text>hello</Text>
    </View>
  )
}

export default SameDateBookings

const styles = StyleSheet.create({})