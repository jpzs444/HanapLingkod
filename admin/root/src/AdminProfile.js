'use-strict';


function AdminProfile () {
    
    
    
    return (
        <div></div>
    )
}


const rootNode = document.getElementById('rootAdminProfile');
const root = ReactDOM.createRoot(rootNode);
root.render(React.createElement(AdminProfile));